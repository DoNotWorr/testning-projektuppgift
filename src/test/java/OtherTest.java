/*
import org.junit.jupiter.api.Test;

import java.util.regex.*;


//Author: MrKnaz
//Checking out what this code does

public class OtherTest {
    String getString(String str) {
        return getMatcher(str).replaceAll("\\$0");
    }

    private Matcher getMatcher(String str) {
        System.out.println("Matcher: " + getPattern().matcher(str));
        return getPattern().matcher(str);
    }

    private Pattern getPattern() {
        System.out.println("Pattern: " + Pattern.compile("[{}()\\[\\].+*?^$|]"));
        return Pattern.compile("[{}()\\[\\].+*?^$|]");
    }

    @Test
    void test() {
        String input = "[*]\\";
        System.out.println("String: " + getString(input));
        System.out.println("Input: " + input);
    }
}
*/
